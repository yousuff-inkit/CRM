# Server
PORT=5000
NODE_ENV=production

# Session secret – change this to a long random string in production
SESSION_SECRET=crm-secret-please-change-me

# CORS – comma-separated list of allowed origins (use * to allow all, not recommended in production)
ALLOWED_ORIGINS=http://localhost:5000
